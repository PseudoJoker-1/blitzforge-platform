# WoT Blitz Mod API — UltraQA test matrix

Цель: динамически проверить каждую публичную функцию ABI 2.9 и runtime API,
включая ошибки аргументов, ownership, лимиты, cleanup и повторный запуск.

## Current verified snapshot — 2026-08-02

Свежие `build.cmd` и `loader\build_live.cmd` завершились с exit code `0`.
Текущий локальный результат: core `105/105`,
C++ wrapper `128/128`, package `68/68`, gameplay `63/63`, DVPL `17/17`, data
`272/272`, interface `72/72`, tooling `269/269`, named permissions `155/155`,
data-named permissions `90/90`, operation
permissions `327/327`, truthful client `365/365`, native/public UI
`49/49` и `66/66`, renderer `32/32`, CLI `17/17`, полный API
`1651 assertions`, publish self-test `92/10/0`, audio `24/0`, new events
`14/14`, skin `15/15`. Runtime services прошли с publisher availability gate,
честными async ingress и hook-ordering результатами; HTTP прошёл `106/106`,
completion доставлен на `RENDER`.
Отдельно: crash-loop recovery `84/84`, native validation `194/194`, frozen ABI
tables `46/46`, RC1 samples `62/62`, validation bundle `29/29`. Полный API host
ранее прошёл stress `10/10`, client subscription/backend harness — `30/30`.

Добавлено 2026-08-16: `build_v3_post_rc1_services_tests.cmd` —
`283 passed, 0 failed`. Это прогон в изолированном scratchpad-каталоге поверх
свежескомпилированного `client_services.cpp`, а не полный `build.cmd`: сам
`build.cmd` этой задачей не запускался и артефакты в `build\` не
перезаписывались. Suite зарегистрирован в делегированной цепочке `build.cmd`
после сборки `build\wotb_mod_runtime.lib`, потому что линкуется с ней.

`wotbmod.py doctor` подтвердил exact WoT Blitz `11.19.0.834` x86/SHA match и
установленный proxy. Отдельный root `wotb_mod_loader.dll` отсутствует. Процесс
игры в этой итерации не был запущен, поэтому все live-значения в таблице ниже
являются явно историческим evidence, а не свежим прогоном.

| ID | User/attacker model | Сценарий | Harness/команда | Ожидаемый сигнал | Фактический результат | Исправления | Evidence | Cleanup |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| API-001 | Loader до запуска runtime | Host/version exports, `LoadAll`, resolve, generation, frame и shutdown до initialize | `api_full_host.exe` pre-init phase | Экспорты валидны; stateful calls возвращают `DISABLED`; void calls безопасны | PASS | — | `API FULL OK`; pre-init assertions | Permanent harness |
| API-002 | Loader с malformed options | `Initialize`: null/короткая структура, некорректные директории, повторная инициализация | `api_full_host.exe` init phase | Точные `INVALID_ARGUMENT`/`PLATFORM`/`ALREADY_EXISTS`; успешное восстановление | PASS | `EnsureDirectory` теперь отличает файл от каталога | `API FULL OK`; recovery cycle | No temporary path created |
| API-003 | Обычный loader | `LoadAll`, alphabetical discovery, повторный вызов, `get_mod_count`, полная и короткая `get_mod_info` | Happy-path mod set | Все корректные DLL загружены; повтор возвращает `ALREADY_EXISTS`; short-copy contract соблюдён | PASS | — | 5 DLL; ordered metadata assertions | Permanent fixtures |
| API-004 | Мод с lifecycle callbacks и аварийный мод | Enable/disable/unload/frame, frame payload, SEH isolation | Contract/fault mods | Callback order/counts корректны; faulted mod изолирован и не включается повторно | PASS | — | Frame payload + fault counters + unload logs | Permanent fixtures |
| API-005 | Мод, пишущий обычный и форматированный лог | `log`, `wotbmod_logf`, null message/invalid handle | Capturing log sink | Уровень, mod id и форматированный текст доставлены без падения | PASS | — | Captured `contract:formatted:42:ok` | In-memory capture |
| API-006 | Мод с валидным/невалидным handle и малыми buffers | `get_path` для GAME/MODS/MODULE/DATA/CONFIG | Contract mod + host assertions | Двухшаговый contract, абсолютные пути и точные ошибки | PASS | — | Size-query/short/exact assertions for all 5 paths | No artifacts |
| API-007 | Мод, разрешающий symbols | `get_game_module`, `resolve_rva`, `get_proc_address`, `find_pattern` | Signature embedded in host image | Base/RVA/export/pattern найдены; invalid/out-of-range/malformed mask отклонены | PASS | Добавлена строгая проверка mask `x/?` | Exact/wildcard/invalid pattern assertions | No artifacts |
| API-008 | Два конкурирующих мода | Все hook calls: create/enable/disable/remove, duplicate, ownership, backend unavailable, 64-slot limit и churn | Fake hook backend | Ownership и лимиты соблюдены; удалённые slots повторно используются; cleanup вызывает disable/remove | PASS | Переиспользование slots; retry cleanup; запрет create/enable у disabled-мода | 80 churn + 64 concurrent + backend error assertions | Backend records cleared |
| API-009 | Мод с отсутствующим и существующим INI | Все config get/set int/string, defaults, exact/short buffer и invalid args | Contract mod config file | Round-trip без потерь; exact-fit не считается truncation; invalid args отклонены | PASS | Исправлено exact-fit/empty-buffer определение truncation | Int/string/default/exact/short assertions | INI ограничен build env |
| API-010 | Loader UI | `set_mod_enabled` для valid/unknown/null/faulted, idempotence и сохранение `mods.ini` | Contract + fault mods | State/callback/config persistence соответствуют contract | PASS | Cleanup error теперь возвращается caller и допускает retry | Callback counters + `mods.ini` assertions | Fixture reset by `build.cmd` |
| API-011 | Мод с hostile mount inputs | mount/unmount: normalization, flags, traversal, absolute path, missing dir, priority/tie, ownership, 32-slot limit | Resource fixtures + two handles | Path escape запрещён; приоритет deterministic; foreign access denied; limit exact | PASS | Disabled owner получает `DISABLED` | 32 fill + overflow + ownership assertions | Все mounts сняты |
| API-012 | Game file resolver | `resource_resolve` и runtime resolver: null/short buffer, all path styles, loose/DVPL fallback, traversal, disabled mounts | Contract/resource fixtures | Точный buffer contract; highest active mount; unsafe paths rejected | PASS | — | Loose/DVPL, absolute Data, oversized/traversal, 4-thread stress | All mounts disabled/unmounted |
| API-013 | Мод с typed assets и stale handles | load/reload/release для всех 7 типов, normalization/object/flags, backend missing/fault, foreign/stale handles, 64-slot limit | Fake resource backend | Backend получает корректный request; opaque handle не оживает после reuse; cleanup без утечек | PASS | Уникальные handle ids; release сохраняется для retry при backend error | 7 types + 64 limit + SEH + zero allocation balance | Все native allocations freed |
| API-014 | Мод со звуковыми ресурсами | Все 7 Audio API calls, defaults, loop/spatial/start-paused, state, ownership, stale handles, clip retention, 64-slot limit, optional/missing backend и SEH faults | Fake audio backend + typed audio clips | Playback управляется только владельцем; параметры валидируются; clip живёт дольше playback; cleanup выполняется audio-before-resource | PASS | Добавлены уникальные playback ids, cached state fallback и retry release | Play/pause/resume/stop/set/get/release, 64 fill, backend variants, zero playback balance | Все native playbacks freed |
| API-015 | Невалидные DLL/descriptor | Missing entry, bad ABI/size, invalid/duplicate id, entry failure/SEH | Isolated malformed-mod environment | `LoadAll` сообщает aggregate failure, успешные моды остаются доступны, failed DLL очищены | PASS | — | 8 discovered, 1 loaded, 7 rejected by intended branches | Isolated env regenerated |
| API-016 | Пустая установка и отсутствующие/старые backends | Empty mods directory; contract mod без hook/resource/audio backend; ABI 2.2 short audio и ABI 2.4 short resource struct | Secondary runtime cycles | Empty load успешен; backend-dependent calls дают точные ошибки; старые structs принимаются | PASS | Prefix-copy audio/resource backends для minor ABI compatibility | No-backend, no-reload, no-audio, legacy audio/resource, empty cycles | Runtime shutdown between cycles |
| API-017 | Повторный/нестабильный запуск | Полный suite и regression runs | `build.cmd` | Exit 0; нет stale runtime state | PASS | — | Свежий ABI 2.9: exit 0; full API 1651; self-test 92/10/0; audio 24/0; events 14/14; skin 15/15 | Build artifacts intentional |
| API-018 | Мод с произвольным audio file | `audio_clip_load`, mount resolution, custom callbacks без generic resource backend, real WAV decode и XAudio2 playback | Fake custom-file backend + `WotbModWindowsAudio_*` | Только mounted loose file загружается; clip lifetime защищён; playback control и cleanup корректны; spatial отклонён ready backend | PASS | ABI 2.3 clip callbacks и готовый MF/XAudio2 backend | Load/reload/play/loop/pause/resume/state/volume/pitch/pan/stop/release/destroy; invalid/missing paths | WAV fixture ограничен build env; native balances zero |
| API-019 | Пользователь устанавливает test packages | Самостоятельный 76-function self-test, generated WAV audio self-test и native ZIP packaging | `published_selftest_host.exe` + package verification | Моды завершают сценарии без FAIL; package содержит manifest, DLL и заявленные resources | PASS | Self-test обновлён до ABI 2.9: skins, existing UI, vehicles и all-event subscription | Свежий standalone API 92 PASS/10 SKIP/0 FAIL; Audio 24 PASS/0 FAIL | Build artifacts regenerated locally |
| API-020 | Мод использует родной `DAVA::SoundSystem` | Все 12 `sound_event_*` calls, invalid args, ownership, stale handles, 64-slot limit, optional callbacks, backend errors и SEH | `api_full_host.exe` с fake native-sound backend | Lifecycle event полностью управляем; чужой/stale handle отклоняется; cleanup и retry не оставляют allocations | PASS | ABI 2.4; уникальные ids, tracked lifecycle state и retry-safe release | Входит в `API FULL OK: assertions=1651`; баланс native events равен нулю | Все события released |
| API-021 | Исторический ABI 2.9 live baseline с реальным `SoundSystemProxy` и DAVA objects | Инициализация loader; native sound lifecycle; реальные `UIPackage`, named/direct `UIControl`, `Entity`, `Scene`; UI/Scene operations, events и hooks | Сохранённый исторический `live_host.log` | Клиент responsive; object lifecycle/operations и детерминированные API проверки без FAIL | HISTORICAL PASS / NOT RERUN | DAVA constructors/vtable operations, Transform layout/RVA, active-root tracker, Present-thread dispatch | Предыдущий client 11.19: API 108/2/0; custom audio 24/0; native 142/0/0; Wwise 1931, UI 206, Scene load 38 hits | Клиент в текущей итерации не запускался |
| API-022 | Мод вызывает UI/Scene mutations из разных потоков | Все 8 ABI 2.5 calls; geometry/transform validation; ownership/type/stale handles; sync dispatch; worker queue; FIFO 64; disable cancellation; callback/backend SEH | Fake object backend + worker thread + repeated `DispatchFrame` | Main-thread calls дают реальный result; worker calls выполняются позже; callback не переживает disable/DLL; fault изолирован | PASS | Owner-aware fixed queue, execution-time handle resolution, legacy resource-backend prefix copy | Входит в `API FULL OK: assertions=1651`; object allocation balance zero | Queue empty after disable/shutdown |
| API-023 | Мод создаёт native UI/scene objects и получает active roots | Четыре ABI 2.6 calls; null/short inputs; dispatch-thread restriction; ownership; transform; reload rejection; active Scene absent/present; custom `.sc2` attach/hold/detach/release | Fake object backend, standalone self-test и исторический ABI 2.9 live baseline | Factory/getter возвращает mod-owned opaque handle; worker получает `WRONG_THREAD`; runtime-created/acquired reload даёт `PLATFORM`; отсутствие active Scene даёт `NOT_FOUND`; после активации модель живёт в Scene без потери getter | PASS | Client allocator + real constructors; active screen Retain; Scene Activate/Deactivate/Draw tracker | Full host 1651; свежий standalone API 92/10/0; исторический live native 142/0/0 и custom SC2 180 frames | Все созданные/полученные handles detached/released |
| API-024 | Мод подписывается на UI/Scene transitions | Три ABI 2.7 calls; masks; FIFO/sequence; payload; main-thread delivery; borrowed deny/stale; clone; ownership; 16-slot limit; unsubscribe; disable/fault cleanup; callback/backend SEH | Fake backend contract suite + standalone self-test + исторический ABI 2.7 live baseline | События доставляются до `on_frame`; borrowed handle не освобождается и не переживает callback; clone переживает callback; fault изолирован | PASS | Bounded event queue, per-mod subscriptions, two borrowed slots, backend wrapper clone, loader UI/Scene ingress | Full host 1651; исторический live: UI events 7, Scene activated 2, deactivated 1 | Очередь и subscriptions пусты после disable/fault/shutdown; clones released |
| API-025 | Мод меняет существующий UI Blitz | UI V3 active screen, recursive find, parent/children, snapshot/state/geometry и permission errors | Fake UI tree + bundled DAVA bridge + исторический client 11.19 live run | Traversal возвращает owned handles; game-owned flag/geometry/state читаются; mutation и cleanup корректны | PASS | Проверены x86 layout, FastName ctor, Retain/Release и безопасные vtable slots | Контракт входит в 1651; focused native/public `49/49`,`66/66`; исторический UI hook 206 | Все retained wrappers released |
| API-026 | Мод читает машины и получает все игровые события | Семь vehicle calls; snapshot ownership/clone/stale/limit/thread/fault; typed event payload/FIFO/sequence/borrowed lifecycle | Fake gameplay backend + current loader build | Ни один raw game pointer не пересекает ABI; typed payload и cleanup корректны | PASS / LOADER BUILT | Vehicle registry, gameplay/input/projectile hooks и camera-mode hook | Свежие gameplay `63/63` и full `1651`; старый hook mask `0xFFF` — только исторический live evidence | Battle-only stimuli и реальная смена camera mode в клиенте не выполнялись; owned/event vehicle tokens balance zero |
| API-027 | Мод регистрирует mesh/material/texture resolver для конкретного танка | Registry плюс Vehicle V2 exact-path pack/LOD/apply/state/rollback/release | Full host resolver tests + focused operation harness + guarded hook initialization | Priority deterministic; apply atomic/idempotent; rollback/release восстанавливают stock path | PASS / RESOLVER | Owner-scoped skin registry и guarded `DAVA::File::Create` path | Входит в 1651 assertions; operation `327/327`; skin mod 15/15 | Rendered cached tank всё ещё требует live reload confirmation |
| API-028 | Отдельный мод проверяет новые native events | UI input, shell hit, ammo, aim target, spotted/unspotted, camera mode; typed payload, dispatch thread, sequence, borrowed vehicle get/deny/clone/release | `new_gameplay_events_test_mod.dll` + `new_api_mods_host.exe` + loader build | Все семь масок получены один раз, payload валиден, owned clone освобождён | PASS / LOADER BUILT | Focused ABI 2.9 event mod и deterministic event injector | Свежие 14 PASS/0 FAIL; mask `0x003E1008`; native battle hooks — только в историческом live log, camera hook проверен свежим IDA-анализом | Реальные battle/camera stimuli pending |
| API-029 | Отдельный мод проверяет T-34-85 skin registry | Mount; register/info/disable/enable/release/stale; low/high priority; mesh/material/texture и DVPL alias | `vehicle_skin_test_mod.dll` + `new_api_mods_host.exe` + loader build + publish fixtures | High registration активна; resolver выбирает owner mount для всех трёх assets; это не доказывает native render application | PASS / REGISTRY | Mounted T-34-85 assets и visible checker DDS test package | Свежие 15 PASS/0 FAIL; 3 resolver PASS; исторический live registry PASS | Native visual override не подтверждён |
| API-030 | Разработчик проверяет truth-contract V3 | `ContentApply` для UI/texture/model и audio/hangar/localization; capability reasons; package READY/signature/warnings | `build_v3_data_named_permission_tests.cmd`, `build_v3_interface_truth_tests.cmd`, `build_v3_package_loader_truth_tests.cmd` | UI/texture/model transactional apply/resolve/unapply работают; semantic kinds дают `NOT_SUPPORTED`; vehicle capability различает exact-path skin pack и cached hot-swap; READY не подменяет signature verdict | PASS | File overlays отделены от semantic overrides; loadability и trust разведены | Data `90/90`; interfaces `72/72`; package `68/68` | Overlay providers удалены при unapply; content handles очищены; trust fixture удалён harness |
| API-031 | V3 мод использует events/async/hooks | Zero/partial/full native event-source masks; system topic без publisher; frame/mod topics; dispatch/task/timer thread roles; hook priority/order/call-next | `v3_runtime_services_tests.exe` | Loader открывает только topics с реально установленным ingress; недоступные publishers и `MAIN`/`AUDIO` ingress дают `E_NOT_SUPPORTED`; `RENDER` callbacks выполняются; неподдерживаемый hook chain не возвращает fake success | PASS | Динамическая source mask и publisher availability gate; automatic ingress ограничен `RENDER`; metadata-only hook ordering удалено из success path | `V3 RUNTIME SERVICES OK` | Tasks/timers/subscriptions/hooks очищены runtime cleanup |
| API-032 | REVIEWED мод делает HTTPS request | Exact host permission, DNS/IP validation, limits, cancel/unload и completion role | `build_v3_http_transport_tests.cmd` | Completion доставляется через реально подключённый `RENDER` ingress; без ложного `MAIN` claim | PASS | Completion role синхронизирован с host ingress | `106/106`, callback role `RENDER` | Request/thread/response handles освобождены |
| API-033 | Мод проверяет operation grants и client/native availability | Operation-level permissions; RPC policy; render/backend; projectile V2; vehicle skin V2 | `build_v3_operation_permissions_tests.cmd`; `build_v3_client_truthful_slices_tests.cmd` | Broad interface grant не разрешает mutation; skin pack apply/rollback атомарны; missing replacement не оставляет mount | PASS | VFS missing-path canonicalization, transactional skin packs и truthful native gaps | Operation `327/327`; truthful client `365/365` | Handles/callbacks/overlays очищены |
| API-034 | Разработчик использует diagnostics/profiler | Crash context, spans/counters; callback budget, per-frame CPU/profile/coalescing; owner resource/hook/event inspectors | `build_v3_tooling_v2_tests.cmd` | Callback budget ограничен, coalescible callbacks учитываются/сбрасываются; portable snapshots сохраняются | PASS | Devtools V3 callback profile поверх V2 prefix | Tooling `269/269` | Per-mod profiler/budget/tooling state очищено |
| API-035 | Предыдущая игровая сессия аварийно завершилась во время загрузки мода | Normal marker claim/remove; child `TerminateProcess` из test-mod `DllMain`; safe `LoadAll`; explicit enable; forensic phase/mod; portable-only recovery; repeated crash auto-disable | `safe_mode_recovery_tests.exe` + `safe_mode_crash_mod.dll` | До загрузки DLL safe mode сохраняет diagnostics, возвращает успешный no-op, блокирует enable и не стирает stale evidence; override создаёт новый marker; второй crash того же мода сохраняет history count и auto-disable | PASS | Persistent marker и `crash_history.ini`, lifecycle attribution, fail-closed recovery gate | `84/84`, child exit `73`, `mod_count=0` в safe mode, повторный crash count `2`, финальный marker отсутствует | Изолированный `build\safe_mode_env`; env variables и fixtures очищены |
| API-036 | Автор C++-мода использует безопасную обёртку над C ABI | Typed query всех V3 interfaces; `Status`/`Result<T>`; `OwnedHandle` adopt/retain/clone/copy/move/close/detach/info/is_alive; foreign/stale handles | `build_v3_cpp_wrapper_tests.cmd` | Wrapper не скрывает исходный result code, не переносит STL/exceptions через ABI и сохраняет точный ownership при success/failure | PASS | Exception-free header-only C++17 layer поверх стабильного C ABI | `128/128`, `/W4 /WX /permissive-` | Все owned handles освобождены; runtime shutdown clean |
| API-037 | Package и loose мод запрашивают lifecycle paths/dependencies | Actual DLL install path; package resource root; loose fallback; required/optional/undeclared dependencies | `v3_runtime_services_tests.exe`; `v3_package_runtime_tests.exe` | Paths не зависят от cwd; dependencies переданы до entrypoint; optional missing различим; undeclared mod скрыт | PASS | Loader-only package-root/dependency handoff с bounded copied descriptors | Runtime services `PASS`; package runtime `PASS` | Metadata очищена при owner unload/shutdown |
| API-038 | Мод асинхронно загружает и перезагружает resource | Shared worker, state/progress/error, owner cancel, saturation, exception, reload rollback, native input ingress | `v3_data_truth_tests.exe` | Handle остаётся валиден до terminal state; input queue/action/capture/coalescing работают; safe missing VFS path остаётся `NOT_FOUND` | PASS | Owner-aware worker/input queues и missing-tail canonicalization | Data truth `272/272` | Workers joined; handles, captures и snapshots освобождены |
| API-039 | Backend заменяется одновременно с callback/subscription lifecycle | Unregister/destroy barriers; publish race; backend generation; reentrant frame pump; release retry | `v3_client_truthful_tests\test.exe` + stress | Нет callback после owner stop; старое поколение не публикуется в новое; replacement не deadlock-ится | PASS | Transition lock отделён от frame lock; invocation quiescence; transient release retry | Focused `365/365`; stress `30/30` | Subscriptions/native objects/retry queue пусты |
| API-040 | Пользователь вручную подтверждает native API на точном client fingerprint | 50 capability, 16 panel sections, PASS/FAIL/SKIP/RESET, privacy JSONL, crash marker, package fingerprint, deferred reload policy | `build_native_validation_tests.cmd` + `wotbmod.native_validation` | Host contract проходит; native status не выше `LIVE_TEST_PENDING` до ручного PASS; verdicts другого fingerprint не импортируются | HOST PASS / LIVE PENDING | Stable binding IDs; exact-build manifest; machine-readable evidence bundle | `194/194` focused assertions; live client намеренно не запускался | Runtime files создаются только live; stock tracer objects, DAVA parser/archive objects и live NMaterial остаются `E_NOT_SUPPORTED` |
| API-041 | V3 мод использует расширенный client API | Projectile lifecycle/impact visual; UI V3 tree/snapshot/layout/events; input ingress; managed draw_mesh; vehicle V2 LOD/rollback; callback budget; binding report; package trust | `build.cmd`, focused V3 harnesses | Prefix ABI компилируется; callbacks bounded; overlays/visuals owner-clean; every RVA validates before hook; invalid/untrusted signatures follow policy | HOST PASS / LIVE PENDING | UI/Projectile/Vehicle/Devtools versioned tables, validation JSON, ECDSA trust store | Gameplay `63/63`; UI native/public `49/49`,`66/66`; operation `327/327`; tooling `269/269`; package `68/68` | Игра не запущена; stock tracer style, DAVA YAML/archive object bridge, hot cached NMaterial и semantic audio/hangar/localization требуют отдельного safe native backend |
| API-042 | Разработчик принимает замороженный V3 RC1 | Public file/interface/permission/schema snapshot; 3 samples; signed revocations; safe-mode recovery; privacy-safe evidence export | `verify_rc1_contract.py`, ABI/sample/export harnesses, полный `build.cmd` | Contract drift останавливает build; samples валидны; revoked key/release и downgrade отклоняются; bundle не содержит secrets/payload; crash loop auto-disables owner | HOST PASS / LIVE PENDING | 56 files, 45 interfaces, 51 permissions, 51 x86 tables, 3 sample packages | ABI `51/51`; samples `62/62`; bundle `29/29`; safe mode `84/84`; package `68/68`; 145 snapshot records | Sample packages unsigned; native verification остаётся ручной; публичное расширение после freeze запрещено без новой версии |
| API-043 | Мод в portable-сборке спрашивает пять интерфейсов, объявленных 2026-08-16 | `wotbmod.ui.read`, `wotbmod.camera.state`, `wotbmod.audio.intercept`, `wotbmod.scene.enumerate`, `wotbmod.tracer` без native backend: статус регистрации, `query_interface`, каждый slot, struct/version validation, fingerprint gate и all-or-nothing группы | `build_v3_post_rc1_services_tests.cmd` | Все пять зарегистрированы `UNAVAILABLE` с собственной причиной; `query_interface` отказывает и очищает таблицу; каждый slot возвращает `NOT_SUPPORTED` с именем операции; backend без доказанного fingerprint отвергается целиком; половина группы отбрасывается целиком | PASS | Portable-половина пяти интерфейсов и восстановление замороженных причин при снятии backend-а | `283/0` в scratchpad-прогоне; пять причин попарно различны и восстанавливаются побайтово | Test double снят, `disarm` вызван ровно один раз, счётчики backend-а после снятия не растут |
| API-044 | Backend отвечает — проверяется семантика, которую нельзя увидеть без ответа | Portable test double (никакого RVA и offset): provenance `_SET`-флагов ui.read; раздельная достоверность двух половин камеры; порядок/подстановка/подавление перехвата звука; два прохода обхода Scene, три cap-а и отказ по потоку; граница таблицы трассеров | `build_v3_post_rc1_services_tests.cmd` (фаза 2) | «Не записано» отличимо от «записано пустым» и от «нет backend-а»; backend, знающий одну половину камеры, не делает вторую известной; `0 = SNIPER`; обход off-thread отвергается **до** вызова провайдера; код `>= 25` — аргументная ошибка, а не clamp | PASS | Раздельные флаги достоверности, staging-буфер вместо буфера мода, валидация каждой записи обхода | Входит в те же `283/0`; статус остаётся `DEGRADED`, а не `AVAILABLE` | Все подписки перехвата сняты; runtime shutdown чистый |
| API-045 | Тест, который прошёл бы и без проверяемой защиты | Negative control: в scratchpad-копии `client_services.cpp` сломаны ровно две защиты — `ClampWalkLimit` возвращает запрошенное без clamp, и код снаряда клампится вместо отказа | Мутантные сборки того же suite в scratchpad | Каждая мутация обязана уронить suite ненулевым кодом | PASS | — | Мутант scene caps: `271/12`, exit `1`; мутант tracer bound: `277/6`, exit `1`; исходный код: `283/0`, exit `0` | Мутанты остались в scratchpad; дерево не изменялось |
| API-046 | Loader подключает exact-build DAVA extensions без выдачи engine pointers модам | Private ABI version/size/fingerprint gate; all-or-nothing YAML/archive/NMaterial/mesh/tracer/class groups; owner/kind/stale token checks; release retry; concurrent backend replacement; YAML/archive snapshot | `build_v3_dava_native_registry_tests.cmd`, `build_v3_data_truth_tests.cmd`, `build_v3_vfs_mount_fault_tests.cmd`, полный `build.cmd` | Provider token не покидает registry; cross-owner/stale/type mismatch отклоняются; snapshot переживает снятие backend; отсутствующая exact capability возвращает `NOT_SUPPORTED`; class registry принимает только reviewed allowlist | HOST PASS / LIVE PENDING | Loader-private `WotbModDavaNativeBackend` v1; exact provider выставляет только `DAVA::UIControl`/`DAVA::Entity` class factory | Registry `87/87`; data truth `368/368`; VFS fault `19/19`; clean `build.cmd` exit `0` и exact final banner PASS | Provider objects освобождены сразу после snapshot или owner cleanup; live YAML/archive/NMaterial/mesh/tracer не запускались |
| QA-001 | Dirty worktree | Не скрыть существующие изменения | Pre/post filesystem inventory | Нет reset/overwrite несвязанных файлов | Game root содержит пустой `.git`, поэтому git diff недоступен | — | File inventory | No cleanup needed |
| QA-002 | Hung command | Ограничить компиляцию и e2e | Bounded commands + background status polling | Команды завершаются либо диагностируются как hung | PASS | — | Full build и live-loader build завершены с exit 0 | Resolver/worker thread handles joined/closed |
| QA-003 | Misleading success output | Не доверять строке `OK` при non-zero exit | Проверка exit code каждой команды | Success только при exit code 0 и отсутствии failure markers | PASS | — | Первые runs с `SMOKE OK` и exit 1 были признаны failed | No artifacts |
| QA-004 | Prompt injection / cancel-resume | Неприменимо к native C ABI; проверить lifecycle substitute | Runtime shutdown/reinitialize cycle + UltraQA state read/clear | Runtime не сохраняет stale state; QA state очищен после завершения | PASS | — | 8 runtime cycles в одном процессе; OMX state clear выполнен | State cleared at completion |

Contract stop condition: host-части API-001…API-046 прошли, свежие `build.cmd`
и `loader\build_live.cmd` имеют exit code `0`, backend allocation balance
равен нулю. Предыдущий ABI 2.9 hangar run на 11.19 остаётся историческим evidence;
свежего live-run не было, потому что процесс игры не запущен. Для
окончательной полевой пометки остаются реальные battle stimuli и native
visual application stock mesh/material/texture отображаемого танка.

Typed UI/SC2 requests проходят через production DAVA object bridge:
создаются реальные `UIPackage`, `UIControl`, `Entity` и `Scene`; ABI 2.5
вызывает geometry/visibility/transform/add/remove через родные DAVA методы,
ABI 2.6 создаёт объекты и получает active roots с отдельным ownership, а ABI
2.7 доставляет UI/Scene transitions через borrowed handles и явный clone.
ABI 2.8 обходит существующее UI-дерево, выдаёт vehicle snapshots и доставляет
typed gameplay events. ABI 2.9 добавляет owner-scoped exact-path replacement
stock `.sc2`/material/`.tex`. Public handles по-прежнему opaque и не раскрывают
сырой DAVA/BigWorld pointer.
